# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Build artefacts
build/
dist/
*.spec.bak

# Model weights (use model_manager to fetch; do not commit)
resources/models/*.onnx
resources/models/*.pt
resources/models/*.pth

# Bundled binaries (fetch at build time)
resources/ffmpeg/*.exe
resources/ffmpeg/*.dll

# 使用者專案輸出
*.zgproj/

# Claude Code 個人設定
.claude/settings.local.json

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Signing secrets (never commit)
packaging/*.pfx
packaging/*.p12
packaging/secrets.*

# Logs / local settings
*.log
.env
.env.local

# Zero-shot detector runtime artifacts (outputs of scripts/zero_shot_detect.py
# and annotate_events.py). Do NOT commit; reproducible from the script + video.
zs_*.json
zs_*.mp4
zs_*.png
zero_shot_*.json
zero_shot_*.mp4
zero_shot_*.png
annot_sample_*.png
annotate_taiwan.log
debug_*.png
taiwan_frame_*.png
taiwan_preview_*.png
taiwan_events_annotated*.mp4

# Auto-downloaded ultralytics weights (re-fetched on demand)
/yolo11n.pt
/yolo*.pt

# Demo project directory
demo.zgproj/

# 實驗產物(YOLO-seg 10min 驗證) — 只擋 repo 根目錄,別擋 scripts/presets 的 preset
/yolo_seg_*.mp4
/yolo_seg_*.json
/yolo_seg_*.log
yolo_seg_smoke/
yolo_seg_event1/
annot*.jpg
annot10_*.jpg

# Pipeline CLI artifacts
zero_shot_events.json

# UI preview renders(smoke 截圖,不入庫)
preview_*.png
